import { defineConfig } from 'vite';

// Placeholder Vite config; not wired yet. Safe to keep around.
export default defineConfig({
  root: '.',
  build: {
    outDir: 'dist',
    emptyOutDir: true,
  },
});
